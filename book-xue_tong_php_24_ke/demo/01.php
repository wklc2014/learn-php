<?php
  /**
   * 1. 函数定义和调用
   * 2. 函数之间传参（实参、形参）
   *   参数传递方式：
   *   - 按值传递
   *   - 按引用传递
   *   - 默认参数
   * 3. 函数返回值
   * 4. 变量函数
   * 5. 对函数的引用
   * 6. 取消引用 unset()
   */

  /**
   * 变量函数库
   * empty();
   * gettype();
   * intval();
   * is_array();
   * is_int();
   * is_numeric();
   * isset();
   * print_r();
   * settype();
   * unset();
   * var_dump();
   */

  /**
   * 字符串函数库
   */

  // $data = "用户名:明日科技:密码:12345**:电话:13591993996";
  // list($user, $users, $pass, $passs, $tel, $tels) = explode(":", $data);
  // echo $users . '<br />';
  // echo $passs . '<br />';
  // echo $tels . '<br />';

  /**
   * 日期时间函数库
   */
  // $date = checkdate(7, 1, 2008);
  // echo $date;
  // var_dump($date);

  /**
   * 数学函数库
   */

  /**
   * 文件系统函数库
   */

  /**
   * MySQL 函数库
   */
?>
